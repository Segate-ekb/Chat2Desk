
Функция ПутьКВложениюGET(Запрос)
	НазваниеШаблона = Запрос.ПараметрыURL["ПутьКВложению"];
	Если не ЗначениеЗаполнено(НазваниеШаблона) Тогда
		Возврат новый HTTPСервисОтвет(400);
	КонецЕсли; 
	Ответ = Справочники.C2D_Вложения.ОбработатьВызовВебСервиса(НазваниеШаблона);
	Возврат Ответ;
КонецФункции
